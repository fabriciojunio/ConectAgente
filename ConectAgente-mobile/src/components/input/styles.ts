import { StyleSheet } from "react-native";
import { Input } from ".";

export const styles = StyleSheet.create({
    Input: {
        width:"100%",
        height: 52,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: "#183273",
        padding: 12,
        fontSize: 16,
    },
})